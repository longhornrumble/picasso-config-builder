# Test Environment Configuration
# Uses local dev server with REAL S3 access

# S3 Configuration - REAL BUCKET
VITE_S3_BUCKET=myrecruiter-picasso
VITE_AWS_REGION=us-east-1

# API Configuration - Local dev server with S3 access
VITE_API_URL=http://localhost:3001

# AWS Credentials (for local dev server)
S3_BUCKET=myrecruiter-picasso
AWS_REGION=us-east-1
AWS_PROFILE=ai-developer

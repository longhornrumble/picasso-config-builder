{
  "name": "picasso-config-manager-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for managing Picasso tenant configurations in S3",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "package": "npm ci --omit=dev && zip -r deployment.zip . -x '*.zip' -x 'node_modules/.cache/*' -x 'README.md'",
    "deploy": "aws lambda update-function-code --function-name Picasso_Config_Manager --zip-file fileb://deployment.zip --profile ai-developer"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.911.0"
  }
}
